# P10 Dana Wright — Board Cyber Advisor

**Persona:** Board-facing cyber risk advisor. Very low patience.  
**JTBD:** Executive overview · risk language · board reports · market-presence zero-refs honesty  
**Surfaces read:** `/executive` (`executive-overview.tsx`), `/reports` (`reports-workbench.tsx`), `/trust-safety` (`trust-safety-dashboard.tsx`), claim-safe stack (`claim-language.ts`, reports, badges)

---

## Scores

| Dimension | Score | Rationale |
|-----------|:-----:|-----------|
| **task** | **3.4** | Can assemble a brief: Executive → CSV/print + Reports “Board brief.” But Executive sits under **Setup** (not Operate), board pack type `ExecutiveRiskSummary` is **not in the UI pack picker**, and leadership honesty metrics exist on API/report HTML but not on the exec screen. |
| **clarity** | **4.0** | Risk vocabulary is disciplined: claim-safe remap, Measured vs Heuristic, Fixed ≠ ticket close, Fixed risk band → **“Closed (risk)”**, ClosedWithoutEvidence separated on exec + packs. |
| **trust** | **4.2** | Zero public refs and Wave/MQ **Fail** are productized (API + admin banner + hardcoded trust pack). Refuse/prove language on Trust & Safety. No fake logos/case studies. Capability still short of “sole board system of record.” |
| **delight** | **2.4** | Dense enterprise residual panels, dark-only UI for board projection, Setup-buried exec, “Proof composer” jargon vs board language. Low-patience path is not one-screen board pack. |
| **a11y** | **2.8** | Period control `aria-label`, tabs, confidence `role="status"`, print:hidden chrome. Charts/rings and dark-only limit boardroom use; no light/high-contrast executive mode. |
| **purchase** | **2.6** | **Paid pilot / appendix source: maybe.** **Sole board cyber-risk platform / market-proven vendor: no** until peer refs + routine measured multi-hop + vendor assurance (see CISO panel alignment). |

---

## Board-pack confidence

**Medium-low as sole board pack source; Medium as evidence-backed appendix with caveats.**

| Strength | Code evidence |
|----------|----------------|
| Snapshot HTML/PDF carries path claim honesty, hypothesis mode, honesty-trust metrics | `packages/reports/src/index.ts` (~780–830): “Board and executive decisions should weight measured hops only”; % claims Measured / Fixed survived revalidation |
| Claim-safe path state + remap disclosure on export | Same package: `claimSafeState`, “not claim-safe”, `remapReason` |
| ClosedWithoutEvidence ≠ Fixed in board packs | P04-20 comments + tests in `packages/reports` |
| Executive audience guidance on compose | `reports-workbench.tsx`: “Board-level: control effectiveness…” when audience Executive |
| In-product integrity before share | Composer facts: Inclusion / Freshness / Integrity / Redaction / Delivery |

| Gap | Code evidence |
|-----|----------------|
| UI “Board brief” ≠ dedicated `ExecutiveRiskSummary` pack | `REPORT_TYPE_OPTIONS` = `ValidationSnapshotReport` + `COMPLIANCE_PACK_TYPES` only; schema still has `ExecutiveRiskSummary` (`domain.ts`) with richer board template copy |
| Exec overview does **not** surface `honestyTrust` | API `getExecutiveTrends` builds summary with honesty fields (tests); `executive-overview.tsx` never reads `honestyTrust` |
| Trust page does **not** bind API `marketPresence` | `TrustSafetySummary.marketPresence` returned (`runtime-services.ts` ~14801); **zero** `s.marketPresence` usage in `trust-safety-dashboard.tsx` (hardcoded zero-ref row + Admin banner only) |

---

## Top 5 findings

### 1. Claim-safe risk language is board-defensible (strength)
**Evidence:** `packages/shared/src/claim-language.ts` — `projectPathValidationState` remaps overclaimed Reachable/Validated/Exploitable → Discovered with explicit `remapReason`. Reports project claim-safe state into HTML/PDF. UI: `RiskBandBadge` / `formatRiskBandDisplayLabel` → **“Closed (risk)”** so path risk Fixed ≠ remediation Fixed; exec velocity bar labels **“Closed w/o evidence”** separately.  
**For Dana:** She can tell the board “Fixed only means measured re-test; ticket close is not Fixed.” That is rare and valuable.

### 2. Executive overview is usable—but not the board cockpit
**Evidence:** `/Volumes/DataSSD1/test/periscan/apps/web/src/components/executive-overview.tsx` — period 30/90/365/all, real deltas via `computeExecutivePeriodMetrics`, Export CSV + Print, “Decision confidence limited” banner, readiness ring with stage legend, SLA discipline tiles, remediation velocity, top paths.  
**Nav:** `primary-nav.tsx` places **Executive under Setup**, not Operate (`/reports` is Operate).  
**Gap:** Differentiator metrics (`honestyTrust`: % Measured, Fixed revalidation, denied-never-queued) live on trend schema/tests and report HTML, **not** on the exec Summary tab—so Dana’s one glance misses the trust story.

### 3. Reports “Board brief” is a preset, not a first-class board pack
**Evidence:** `reports-workbench.tsx` presets: Board brief = Executive + top 3; generate path creates **Validation Snapshot**, not `ExecutiveRiskSummary`. Compliance packs are in the dropdown; dedicated executive risk / QBR / insurance pack types from schema are **absent from UI**.  
**For Dana:** She can preview sandboxed HTML and share tokenized links—good. She cannot one-click the template that reports package already special-cases for board language.

### 4. Market presence zero-refs honesty is real—but split-brain in UI
**Evidence:**  
- `buildMarketPresenceReadiness()` → `publicReferenceCount: 0`, Wave/MQ/peer **Fail** (`packages/shared/src/domain.ts`).  
- Trust API always attaches `marketPresence` (`runtime-services.ts`).  
- Admin zero-refs banner reads API (`admin-console.tsx` `AdminMarketPresenceBanner`).  
- Trust dashboard: GTM claim panel (`getGtmClaimLanguageSummary`), Enterprise commercial honesty, **hardcoded** “Market presence / customer refs · Zero references (fail)” in `EnterpriseTrustPackPanel`—**no live gates/KPIs from API**.  
**For Dana:** She will **not** invent references (correct). She still cannot show the board a single live “0 refs / Fail” control from Trust that matches Admin’s API banner.

### 5. Low-patience path is too long for board prep
**Evidence:** Trust & Safety stacks Offensive auth → Identity SCIM honesty → Commercial residual → Trust pack docs → GTM prove/integrate/refuse → principles → runner → residency → retention → integrations → readiness. Correct for RFP; wrong as pre-board 5-minute path. Executive is multi-tab + charts without a single “board narrative” export (print/CSV are partial). Dark-only product hurts projector/print board packs.

---

## Would recommend to board?

| Question | Answer |
|----------|--------|
| **Recommend Periscan as sole cyber-risk narrative for the full board pack?** | **No** |
| **Recommend as measured-proof appendix / pilot evidence layer under CISO ownership?** | **Conditional yes** (scoped design partner; disclose zero public refs; never claim BAS/certification) |
| **Recommend citing market presence / peer logos?** | **No** — product correctly fails Wave/MQ market presence |

**Would recommend to board?** **No** (as platform-of-record / market-proven buy).  
**Would recommend for board *appendix* after a controlled pilot with claim-safe language?** **Yes, carefully.**

---

## Scorecard summary (paste)

```
## P10 Dana Wright — Board Cyber Advisor
**Scores:** task:3.4 clarity:4.0 trust:4.2 delight:2.4 a11y:2.8 purchase:2.6
**Board-pack confidence:** Medium-low sole source · Medium as caveated appendix
**Top 5 findings:**
1. Claim-safe / Closed(risk) / ClosedWithoutEvidence language is board-grade
2. Executive improved (period/delta/CSV/print/SLA) but honestyTrust not on-screen; nav under Setup
3. Board brief ≠ ExecutiveRiskSummary; dedicated board pack type missing from UI
4. Zero-refs honesty real (API + admin + copy rules) but Trust UI doesn’t bind marketPresence payload
5. Trust/RFP density + dark UI fail low-patience board prep
**Would recommend to board?** no (sole SoR) · conditional appendix/pilot only
```

### Code anchors (absolute)

| Surface | Path |
|---------|------|
| Executive overview | `/Volumes/DataSSD1/test/periscan/apps/web/src/components/executive-overview.tsx` |
| Reports workbench | `/Volumes/DataSSD1/test/periscan/apps/web/src/components/reports-workbench.tsx` |
| Trust & Safety | `/Volumes/DataSSD1/test/periscan/apps/web/src/components/trust-safety-dashboard.tsx` |
| Claim-safe projection | `/Volumes/DataSSD1/test/periscan/packages/shared/src/claim-language.ts` |
| Board/report honesty HTML | `/Volumes/DataSSD1/test/periscan/packages/reports/src/index.ts` |
| Market presence builder | `/Volumes/DataSSD1/test/periscan/packages/shared/src/domain.ts` (`buildMarketPresenceReadiness`) |
| Zero-refs admin banner | `/Volumes/DataSSD1/test/periscan/apps/web/src/components/admin-console.tsx` |
| Product copy (no fake refs) | `/Volumes/DataSSD1/test/periscan/docs/DESIGN_PARTNER/PRODUCT_COPY_RULES.md` |