## P02 Jordan Blake — SOC Analyst

**Persona:** SOC L2 · dual monitor · low patience · shift work  
**Surfaces reviewed (code):** `/shift` + `blue-shift-brief.tsx`, `/findings` + `findings-workbench.tsx`, `/continuous` + `continuous-validation-hub.tsx`, `/schedules` + `schedules-workbench.tsx`, `live-update-pill.tsx`, `app-shell.tsx` Monday/Operating mode + `primary-nav.tsx`  
**API truth:** `GET /api/v1/experience/shift-brief` → `tenant.getBlueShiftBrief` (wired; post–P18 hollow-wire fix)

**Scores:** task:**4.0** clarity:**4.0** trust:**4.5** delight:**3.0** a11y:**3.5** purchase:**3.5**

**Job outcomes:** JTBD 2,3,6  
| # | Job (Jordan) | Outcome on current tip |
|---|--------------|------------------------|
| **2** | Path/findings triage | **Met for Active queue.** Default `savedView=active` + server `excludeDisposition=FalsePositive,Suppressed`; ×N / `fp·` / root-cause on rows; reason codes + bulk; 50-row offset/`hasMore`; My queue + Priority·unowned use operational ownership. |
| **3** | Continuous schedule | **Partial.** Real pause/resume/run-now + prior diffs on `/schedules`; Needs-you includes paused/missed/failed schedule buckets. `/continuous` is a real-first hub but **not on Operate rail** — mostly deep-link plan/run/health + sales walk, not a shift console. |
| **6** | Shift brief | **Met enough to start shift.** `/shift` on Operate (next to Home), Operating/Activating allow-lists include `/shift`; brief aggregates work-queue buckets + FP-by-reason; 60s poll. Still category-first, not a full chronological case console (case feed lives under Home Needs you). |

---

### Top 5 findings

| Sev | Surface | Issue | Fix |
|-----|---------|-------|-----|
| **P1** | Findings disposition | **Fingerprint mute is silent.** Server defaults `applyToFingerprint` true and sets `inheritedFromFingerprint` (`apps/api/src/services/findings.ts`); web has **zero** `applyToFingerprint` / `inheritedFromFingerprint` UI. Jordan cannot answer “did we mute this root cause?” or opt out. | Checkbox “Mute fingerprint (default on)” on FP/Suppress; badge “Muted via fp”; clear path; optional muted-fp table from disposition feedback. |
| **P1** | Continuous hub / nav | **`/continuous` off Monday Operate rail.** Operate spine: Home · Shift · Connect · Scope · Validate · Paths · Findings · Rem · Evidence · Reports · Schedule (`primary-nav.tsx`). Continuous is orphan deep-link; hub still carries **Sales walk (honest)** block — low-patience L2 tax. | Promote health strip (active/paused/next/missed) into Shift brief or Schedule header; keep full hub under Schedule or Labs. Drop sales walk from analyst path. |
| **P2** | Schedules workbench | **Create form dominates; L2 wants “is continuous healthy?”** `PageShell width="narrow"` opens with multi-type create (mission/freq/tz/blackout) before ops list. Run-now/pause/priorDiffs exist but secondary. | Default list-first “Program health” (Active · next · lastDiff · failed); collapse create behind “New schedule”. |
| **P2** | Findings queue | **Storm-scale friction remains:** select-page only; severity chips non-interactive counts; no `firstSeenAt`/`lastSeenAt` on row/CSV (grep empty in workbench). | Clickable severity chips; “Select all matching (cap N)”; first/last mono meta + CSV. |
| **P3** | Live update / dual monitor | **`LiveUpdatePill` is honest poll UX, not dual-monitor ops.** Findings 45s, shift/dashboard 60s; pill `role="status"` + “Live · updated Ns ago” (`live-update-pill.tsx`). No multi-pane / pop-out; second monitor is just another browser tab. | Optional denser “monitor” layout or deep-link pairs (Findings \| Paths); don’t claim real-time SIEM. |

---

### Code evidence (real-first)

**Monday / Operating rail (Shift in spine, Labs hidden by default)**  
`/Volumes/DataSSD1/test/periscan/apps/web/src/components/app-shell.tsx` — `OPERATING_DEFAULT_NAV` includes `/shift`, `/findings`, `/schedules`; Labs only via “Show Labs & more”.  
`/Volumes/DataSSD1/test/periscan/apps/web/src/lib/primary-nav.tsx` — Operate: Home → **Shift brief** → … → Findings → … → Schedule.

**Shift brief end-to-end**  
- UI: `apps/web/src/components/blue-shift-brief.tsx` → `api.getBlueShiftBrief()`, 60s refetch.  
- Route: `apps/api/src/app.ts` `GET /api/v1/experience/shift-brief`.  
- Service: `apps/api/src/services/tenant.ts` `getBlueShiftBrief` maps `getProductWorkQueue` buckets + `listDispositionFeedback` → `falsePositiveByReason`.  
- Honesty copy: “Deep links only — beside SIEM/ITSM” on `apps/web/app/shift/page.tsx`.

**Findings triage (shift-usable)**  
- Active default + server exclude FP/Suppressed; `PAGE_SIZE = 50` + offset/hasMore; reason select required for FP/Suppressed (single + bulk); suppress revisit date; disposition feedback strip + Blue shift link; `LiveUpdatePill` on header.  
- Row: `×{occurrenceCount}`, `fp·…`, `rootCauseSummary`, unowned/SLA.  
- Disposition cannot claim Fixed (copy + control model).

**Case feed (Home, not Shift)**  
`dashboard-command-center.tsx` renders `workQueue.data.feed`; `tenant.ts` builds entity rows (finding / risk / threat / retest / overdue), not bucket mirrors.

**Continuous**  
Hub: live schedule metrics + links to Shift/Schedules/Threats/Ops; honesty notes for Continuous EASM. **Not** in `PRIMARY_NAV` Operate list.  
Schedules: real create/pause/resume/run-now + prior diffs + ContinuousValidation honesty note.

**Mute gap (trust vs UX)**  
Server mute path real; UI never surfaces inheritance — Jordan’s low-patience failure mode when siblings vanish/reappear.

---

### Shift fit

| Helps a 12h shift | Taxes a 12h shift |
|-------------------|---------------------|
| Shift brief on rail; one open for program health | Continuous hub + sales walk if they land there |
| Active findings, not All noise dump | Create-first Schedules form |
| Reason-coded FP/bulk + sticky bulk bar | Silent fingerprint mute |
| Needs-you + case feed on Home; schedule failure buckets | Poll-only “Live” (45–60s), not push |
| Copy view link for handoff notes | Expand-to-act disposition (no denser dual-pane) |
| Disposition ≠ Fixed honesty | Jargon (missionType enums, ContinuousValidation prose) |

---

### Would use daily?

**yes** — as the **validated-exposure / continuous-proof queue next to SIEM**, not as the SOC platform of record.

Jordan’s Monday loop that works today: **Shift brief → Needs you / Case feed → Findings Active (My queue) → path/evidence/rem → Schedule only when next-run/failed flags fire.**  
He would **not** live in Continuous hub or Labs. Purchase intent is “buy the proof layer for continuous revalidation + triage,” not “replace Splunk/SOAR.”