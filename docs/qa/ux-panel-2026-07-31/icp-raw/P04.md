# P04 Riley Chen — VP Engineering (economic buyer)

**Lens:** time-to-value, engineering cost of ownership, API for automation, runner ops — **not** feature zoo.  
**Surfaces reviewed (code):** get-started, first-run primary action, activation resume, runners/engines/labs, Setup collapse, API reference.

---

## Scores

| Dimension | Score (1–5) | Riley’s read |
|-----------|------------:|--------------|
| **task** | **4.0** | Empty Home is a real 3-step spine; rail CTA and Home share one resolver. |
| **clarity** | **3.6** | Primary path is clear; secondary 9-milestone guide + residual taxonomy noise. |
| **trust** | **3.7** | Strong safety/runner honesty; automation trust still dinged by list-envelope / capability gaps. |
| **delight** | **3.5** | GetStarted + fleet control room feel productized; pairing is Claude-code-simple. |
| **a11y** | **3.6** | Shell landmarks, progressbars, rail `aria-expanded`; not procurement-complete. |
| **purchase** | **3.3** | Design-partner / pilot eng budget yes; platform-of-record eng program not yet. |

---

## Time-to-first-proof estimate (from code flows)

**Definition used:** first **MeasuredResult** = completed validation run with evidence (`getProductActivationState` in `apps/api/src/services/tenant.ts`). Full “Prove” (remediate → re-validate → share/export) is longer.

| Path | Estimate | Why |
|------|----------|-----|
| **UI first measured proof** (connect → authorize → Validation Snapshot) | **~45–90 min** with credentials ready | Single primary CTA spine; no runner required for pure cloud/source-side snapshot if scope + connector work. |
| **+ private runner enrollment** (Docker register + check-in) | **+15–40 min** | Token issue → `docker run … register` → fleet poll; honest empty until first heartbeat. |
| **Full proof loop** (Act → Verify → ProofDelivered) | **½–2 days** | 9 persisted milestones; depends on fix ownership + retest. |
| **API-first automation** (`examples/proof-loop.sh`) | **Hours to green, not minutes** | Needs live key, mission/run IDs; residual contract edges still cost eng time. |

**Resume behavior (first-proof resume):** not a brittle local wizard. Progress is **server-backed** activation milestones + diagnostics; GetStarted retires only after measured validation; post-setup uses `nextAction` and “Continue toward Prove.” Same brain drives rail CTA (`resolveFirstRunPrimaryAction`).

---

## Top 5 findings (Riley-weighted)

### 1. First-run spine is real and shared (strong TTV signal)
**Evidence:**  
- `/Volumes/DataSSD1/test/periscan/apps/web/src/components/get-started.tsx` — Connect → Authorize (`/scopes`) → Run validation (`/missions`); progress from live activation.  
- `/Volumes/DataSSD1/test/periscan/apps/web/src/lib/first-run-primary-action.ts` — single CTA for Home + rail.  
- `/Volumes/DataSSD1/test/periscan/apps/api/src/services/tenant.ts` (`getProductActivationState`) — 9 milestones from DB, not fixtures.

**Riley:** Low cognitive load for “what do I do next?”; eng onboarding cost is bounded if SE sticks to the spine.

### 2. Zoo is gated — Setup collapsed; Labs opt-in (matches “not zoo”)
**Evidence:**  
- `/Volumes/DataSSD1/test/periscan/apps/web/src/lib/primary-nav.tsx` — only **Operate** has `defaultOpen: true`; Setup (Runners, Engines, Getting started…), Labs, Admin collapse by default.  
- `/Volumes/DataSSD1/test/periscan/apps/web/src/components/app-shell.tsx` — New/Activating allow-lists; Labs fully hidden until **“Show Labs & more”**; Engines stay Setup-only.  
- Engines at `/engines` = tool governance marketplace, not Operate hero.

**Riley:** Good progressive disclosure. Residual: **SecurityEngineer** daily set still re-opens Runners/Assets/Trust (`ENGINEER_DAILY_NAV`) — more than a pure Operate rail.

### 3. Runners are productized for ops cost — one LTS story, pairing UX
**Evidence:**  
- `/Volumes/DataSSD1/test/periscan/docs/SUPPORTED_CUSTOMER_RUNNER.md` — Go `apps/runner` = sole production LTS.  
- `/Volumes/DataSSD1/test/periscan/apps/web/src/lib/runner-install.ts` — primary image `ghcr.io/seanheiney/periscan-runner`; agent labeled optional lab.  
- `/Volumes/DataSSD1/test/periscan/apps/web/src/components/runner-fleet-control-room.tsx` + `runner-pairing.tsx` — fleet workspace + one-time token, Docker/K8s/systemd, poll for check-in.  
- Outbound control plane / signed tasks (architecture docs + fleet copy).

**Riley:** Deploy story is understandable; dual binary still exists but is **honestly demoted**, not dual-SKU theater. Residual: control-plane host install URLs (`/install/runner.sh`, `runner.yaml`) are still deploy-guide-shaped (aspirational entry points noted in runner triage).

### 4. API surface is visible and improving — not yet “trust blindly”
**Evidence:**  
- `/api-reference` → `ApiReferenceConsole` from live `getApiReference()` + OpenAPI link (`apps/web/src/components/api-reference-console.tsx`).  
- Idempotency **is wired** on create paths in `apps/api/src/app.ts` (`withIdempotencyKey` + `createPrismaIdempotencyStore`).  
- Webhook rotate/redrive routes mounted.  
- Residual honesty: `listAttackPaths` / remediations still **limit-capped bare `{ items }`** (comments cite P20-2); bulk findings still not the automation path (`examples/proof-loop.sh` loops single transitions); fine-grained API key capabilities still incomplete per security/API panels.

**Riley:** Will fund automation **if** contract = runtime. Will not greenlight a large SOAR/platform investment until list envelopes + least-privilege keys are boring.

### 5. Eng cost of “full product” still high if you leave the spine
**Evidence:**  
- Secondary `/getting-started` 9-milestone guide (`getting-started-guide.tsx`) vs Home 3-step (intentional progressive, still dual surfaces).  
- Large Labs catalog (MCP, gateway, swarm, NHI…) still ship-coded — gated, not deleted.  
- Welcome profile + persona allow-lists add setup tax before proof.  
- Hero coach completion still localStorage residual (B3 first-run note); multi-taxonomy recall still a UX residual.

**Riley:** Pilot eng cost looks acceptable on Connect→Scope→Validate; **platform eng cost** scales with zoo surfaces, identity packaging (SCIM NotConfigured), and any automation built against stale OpenAPI edges.

---

## Would approve budget?

### **Yes — design-partner / pilot eng budget**  
Approve a **bounded** spend: 1 SE + light platform time for connector + scope + one Validation Snapshot (+ optional Docker runner), with success = **MeasuredResult + one re-validate story**. Aligns with ICP wedge (“one measured proof loop this week”).

### **No — unrestricted platform / multi-team eng program (yet)**  
Would **not** approve full “replace our automation plane / BAS zoo / enterprise identity program” budget until:

1. OpenAPI ↔ runtime list envelopes and key capabilities are green end-to-end.  
2. Time-to-first-proof is measured in a real tenant without SE hand-holding.  
3. Labs stay out of default SE mental model in demos (already code-gated — enforce in sales/eng).

---

## Code map (absolute paths)

| Concern | Path |
|--------|------|
| First-run CTA | `/Volumes/DataSSD1/test/periscan/apps/web/src/lib/first-run-primary-action.ts` |
| GetStarted (primary) | `/Volumes/DataSSD1/test/periscan/apps/web/src/components/get-started.tsx` |
| Full activation guide | `/Volumes/DataSSD1/test/periscan/apps/web/src/components/getting-started-guide.tsx` |
| Activation / resume truth | `/Volumes/DataSSD1/test/periscan/apps/api/src/services/tenant.ts` |
| Nav groups + Setup collapse | `/Volumes/DataSSD1/test/periscan/apps/web/src/lib/primary-nav.tsx` |
| Maturity/Labs filtering + rail CTA | `/Volumes/DataSSD1/test/periscan/apps/web/src/components/app-shell.tsx` |
| Runners product UI | `/Volumes/DataSSD1/test/periscan/apps/web/app/runners/page.tsx` → `runner-fleet-control-room.tsx` |
| Install honesty | `/Volumes/DataSSD1/test/periscan/apps/web/src/lib/runner-install.ts` |
| Engines (Setup) | `/Volumes/DataSSD1/test/periscan/apps/web/app/engines/page.tsx` |
| API reference UI | `/Volumes/DataSSD1/test/periscan/apps/web/app/api-reference/page.tsx` → `api-reference-console.tsx` |
| Customer automation example | `/Volumes/DataSSD1/test/periscan/examples/proof-loop.sh` |

---

**One-line verdict for Riley:** *“I’ll fund the proof-loop pilot and private runner; I won’t fund building our internal platform on Periscan’s API until contract honesty and first-proof time are measured boring.”*