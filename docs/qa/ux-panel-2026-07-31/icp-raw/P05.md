# P05 Morgan Patel — MSSP multi-tenant analyst

**Persona:** portfolio clients · findings · remediation verify · isolation honesty  
**Surfaces read:** `/mssp` (`MSSPPortfolioWorkbench`), Findings, Remediation, Admin, primary-nav **Clients**, shell tenant chrome, API portfolio/switch, isolation/Fixed gates.

---

## Scores

| Dimension | Score | One-line |
|-----------|-------|----------|
| **task** | **5.5** | Portfolio + verify law are real; **enter-client hop is missing in UI** |
| **clarity** | **6.0** | Isolation/batch honesty is excellent; operator path is incomplete |
| **trust** | **8.5** | Fixed-only-via-verification + RLS/isolation proof + no cross-tenant bulk |
| **delight** | **5.5** | Exception cockpit is sharp; ends in a review queue with no “open client” |
| **a11y** | **6.0** | Basic aria on batch controls; cards not a full keyboard drill-in flow |
| **purchase** | **4.5** | Would pilot architecture; **would not put a full client book on UI today** |

**Multi-tenant clarity:** **Partial (honest isolation, incomplete operator switch)**  
**Would run customer book on this?** **No** — API/hierarchy yes; daily MSSP book ops no until client enter + provision ship in UI.

---

## Multi-tenant clarity

### What is clear and real

1. **Clients is a first-class Admin nav item** (`/mssp`, label “Clients”, hint “MSSP portfolio”).  
   Evidence: `/Volumes/DataSSD1/test/periscan/apps/web/src/lib/primary-nav.tsx` (Admin → Clients → `/mssp`).

2. **`MsspOperator` persona** prioritizes Clients and first-run CTA “Triage clients”.  
   Evidence: `/Volumes/DataSSD1/test/periscan/apps/web/src/components/app-shell.tsx` (`MsspOperator` allow-list includes `/mssp`, `/findings`, `/remediation`; persona fallback → `/mssp`).

3. **Portfolio is API-real**, not fixture theater: readiness, critical/high paths, open remediations, verification pending, missing proof inputs, white-label branding, report export link.  
   Evidence: `buildMSSPClientPortfolio` / `buildClientPortfolioSummary` in `/Volumes/DataSSD1/test/periscan/apps/api/src/runtime-services.ts`; UI `/Volumes/DataSSD1/test/periscan/apps/web/src/components/mssp-portfolio-workbench.tsx`.

4. **Isolation honesty is product language, not marketing**:
   - Header: multi-tenant real, GTM still one-org loop first.
   - Batch: **“batches review, not mutations”**; actions stay inside one named tenant.
   - Help: portfolio batch is ordering only.  
   Evidence: workbench copy + `/Volumes/DataSSD1/test/periscan/apps/web/src/lib/product-help.ts` (`MSSP_GUIDE`); GTM `/Volumes/DataSSD1/test/periscan/docs/DESIGN_PARTNER/MSSP_GTM_WEDGE.md`.

5. **Shell deliberately does not fake a tenant switcher** (workspace label only).  
   Evidence: `TenantSwitcher` comment in `app-shell.tsx` (P02-14).

### Where clarity breaks for Morgan

| Gap | Why it hurts Morgan |
|-----|---------------------|
| Help step **“Enter the client context”** | No button/link on client cards to open that tenant’s findings/remediation/validate |
| API switch exists; browser client never sets it | `x-periscan-tenant-id` accepted by API + proxy; `PeriscanApiClient.request` has **no** tenant header state |
| Create client is API-only | `POST /tenants/current/clients` exists; **zero** web calls to create clients |
| Findings/Remediation are current-session-tenant only | On parent MSSP tenant, operate rails are the parent’s empty book, not portfolio children |

**Verdict:** Isolation *policy* is clear and trustworthy. Isolation *operator UX* (which client am I in? how do I enter?) is **not** closed.

---

## Top 5 findings

### 1. **P0 — No UI path to enter a client tenant** (task-breaking)

**JTBD break:** Morgan ranks exceptions, then cannot open Customer One’s findings/remediation from the portfolio.

- Client cards: checkbox, stats, optional latest report export — **no “Open workspace” / switch**.
- Product help still says: *“Open the selected tenant and perform each action there…”* (`product-help.ts` MSSP_GUIDE step 3).
- API switch is real: session + membership-checked `x-periscan-tenant-id` (`app.ts` `getRequestedTenantId` → `getSessionContext`; acceptance `enterprise-foundation.test.ts`).
- Proxy forwards the header; **browser client never sends it** (`periscan-api-client.ts` `request`).
- Shell *removed* a chevron that *implied* switch without implementing one.

**Morgan:** Exception cockpit is a ranking whiteboard, not a client book operator console.

---

### 2. **P0 — Portfolio batch is honest but dead-end**

Workbench builds “Tenant-safe batch triage” with ranked reasons (critical paths, re-tests waiting, missing inputs, stale snapshot, no report). Test asserts: *“This queue batches review, not mutations.”*

There is **no** next action per selected client (no deep links to `/findings`, `/remediation`, `/missions` *in client context*).  
Exception rank is useful; **handoff to work is missing**.

---

### 3. **P1 — Client provisioning not in product UI**

| Layer | Status |
|-------|--------|
| API `createClientTenant` / `listClientTenants` | Implemented (roles, parentTenantId, ClientAdmin invite) |
| Admin console | Workspace, members, keys, branding, SSO — **no clients panel** |
| MSSP workbench | Read portfolio only |

Morgan (or partner admin) cannot stand up a book from the UI; only API/scripts/seed.

---

### 4. **P1 — Findings + remediation are excellent single-tenant tools, disconnected from portfolio**

**Findings** (current tenant): server-side Active queue, priority-unowned, disposition reason codes, hop-measurement honesty, bulk disposition — solid analyst queue.

**Remediation**: Fixed only after measured re-validation; UI copy and auto-revalidate refuse fake “config push Fixed”; tickets say external close still needs verification.

**Code law:** `/Volumes/DataSSD1/test/periscan/packages/shared/src/fix-verification.ts` — only `verifyRemediation` / runner result may write RemediationTask `Fixed`.

**For Morgan:** Once *inside* a client, trust on verify is high. **Getting inside** from Clients is the missing step. Parent-tenant Findings/Remediation do not roll up multi-client queues.

---

### 5. **P2 — Isolation leave-behind is real; channel readiness is still “architecture + honesty,” not book ops**

- Isolation proof: Reports / Trust & Safety (“tenant-scoped, not vendor SOC 2”).
- Portfolio uses `runWithoutTenantRls` with strict parent/child scoping.
- GTM doc: dogfood after one measured partner with two children; zero evidenced live books; refuse white-label automated pentest theater.

Trust for *selling isolation* is high. Trust for *running 30 clients day-to-day in this UI* is not.

---

## Surface-by-surface (code evidence)

| Surface | Evidence | Morgan take |
|---------|----------|-------------|
| **Primary nav Clients** | `PRIMARY_NAV` Admin → `/mssp` “Clients” | Discoverable under Admin (collapsed group); persona CTA helps |
| **MSSP portfolio** | `mssp-portfolio-workbench.tsx` + `GET …/client-portfolio` | Real rollups, exception rank, white-label, report link |
| **Findings** | `findings-workbench.tsx` | Strong triage; no multi-tenant/client filter |
| **Remediation** | workbench + detail + `fix-verification.ts` | Verify honesty is a product strength |
| **Admin** | `admin-console.tsx` | Single-tenant admin; not client portfolio admin |
| **Shell workspace** | `TenantSwitcher` display-only | Honest non-switcher; leaves gap vs API switch |

---

## Would run customer book on this?

**No.**

| Need | Status |
|------|--------|
| Parent/child hierarchy + roles | Yes (API/tests) |
| Portfolio exception ranking | Yes (UI) |
| Isolation / no bulk cross-tenant mutate | Yes |
| Fixed only via measured verify | Yes |
| Enter client from UI | **No** |
| Provision client from UI | **No** |
| Cross-client findings/remediation book view | **No** (by design: one tenant at a time — but switch missing) |

**Conditional yes later:** after (1) explicit workspace switch with visible current client, (2) “Open client” from portfolio (sets tenant context for session), (3) create-client + invite in Admin/Clients, (4) keep batch non-mutating.

---

## Score rationale (short)

- **task 5.5** — Rank clients yes; work findings/verify *per client* blocked at chrome.  
- **clarity 6.0** — Copy/help over-promise enter-client; architecture messaging is otherwise excellent.  
- **trust 8.5** — Isolation + Fixed law + no fake bulk are rare and real.  
- **delight 5.5** — Ranking/readiness feel good; loop doesn’t close.  
- **a11y 6.0** — Batch controls labeled; no full card→workspace keyboard journey.  
- **purchase 4.5** — Pilot 1–2 children via API ok; full MSSP book purchase blocked by operator UX.

---

## Minimal product truth for overnight loop (if prioritized for P05)

1. Persist active tenant (cookie/header) + chrome label **“Working as: {client}”**.  
2. Client card primary action: **Open client** → switch + land Findings or Home.  
3. Batch items: same open action (still no multi-tenant mutations).  
4. Clients/Admin: create client + invite ClientAdmin.  
5. Align MSSP help with reality until switch ships (or ship switch and keep help).

Until then Morgan can **trust** Periscan’s isolation story and **rank** a portfolio, but cannot **run** a customer book as an MSSP analyst in the product UI.