# P09 Jamie Brooks — IT Security Manager (mid-market)

**Lens:** Low patience; laptop + phone; JTBD = first-run → first proof resume → Monday triage → honest empties.  
**Mode:** Source audit of current main (no live browser).  
**Closest prior persona:** mid-market practitioner (P14) + blue Monday (P06), updated for current `get-started` / shift / shell code.

---

## Scores

| Dimension | Score | Why (Jamie, 30-second gut) |
|-----------|------:|----------------------------|
| **task** | **3.4** | Can start first proof and open a Monday inbox; Act loop still frays (path-gated rem, dense phone ops). |
| **clarity** | **3.5** | One strong first-run story on Home; secondary 9-milestone guide + persona rail still steal attention. |
| **trust** | **4.2** | Honest empties, measured Fixed language, real shift brief (no hollow 404). |
| **delight** | **3.0** | GetStarted aurora/radar lands; Shift + workbenches feel utilitarian; phone is “check,” not “run.” |
| **a11y** | **3.6** | Shell drawer: trap, inert, 44px menu targets; product boards still desktop-first. |
| **purchase** | **3.3** | Buyable as proof layer beside SIEM/ITSM for design-partner week; not yet “replace my Monday stack.” |

**Mobile / first-run fit:** **B-**  
Shell + GetStarted are phone-aware; Validate / Findings / Remediations are not. First-run resume is solid on laptop.

---

## Mobile / first-run fit (code)

| Surface | Fit | Evidence |
|---------|-----|----------|
| **First-run Home** | Strong | Empty program → only `GetStarted` (no wall of zeros). |
| **Resume / primary CTA** | Strong | Shared `resolveFirstRunPrimaryAction` for hero + rail. |
| **Monday mode** | Good (thin) | `/shift` + dashboard “Needs you” + Operate “Shift brief”. |
| **Mobile nav** | Good | Hamburger, dialog drawer, focus trap, route close, `inert`. |
| **Empty findings/paths** | Excellent | Honest copy + next CTA; no fake Measured. |
| **Phone work** | Weak | Tables/filters/fix flows are dense; not thumb-first. |

---

## Top 5 findings

### 1. First-run spine is real — resume is milestone-backed (keep)

Empty tenants get a single Home board: connect → authorize → validate, progress from activation, retires when program data exists.

```481:492:/Volumes/DataSSD1/test/periscan/apps/web/src/components/dashboard-command-center.tsx
  // P02-1 residual: single first-run surface on Home = GetStarted only.
  // GettingStartedGuide lives at /getting-started and is linked from GetStarted
  // footer ("Full activation guide →") — never mounted here as a second board.
  // Retires once any snapshot/path/finding exists (wait for load to avoid flash).
  const coreLoading = paths.loading || findings.loading || snapshots.loading;
  const programStarted =
    (snapshots.data?.length ?? 0) > 0 ||
    (paths.data?.length ?? 0) > 0 ||
    (findings.data?.length ?? 0) > 0;
  if (!coreLoading && !session.error && !programStarted) {
    return <GetStarted userName={session.data?.user?.name} />;
  }
```

```131:173:/Volumes/DataSSD1/test/periscan/apps/web/src/components/get-started.tsx
  const steps: Step[] = [
    {
      n: 1,
      title: "Connect a signal source",
      ...
      href: "/integrations",
      ...
    },
    {
      n: 2,
      title: "Authorize scope",
      ...
      href: "/scopes",
      ...
    },
    {
      n: 3,
      title: "Run your first validation",
      ...
      href: "/missions",
      ...
    }
  ];
  // P02-5: single primary CTA brain shared with the rail (setup spine → nextAction).
  const primary = resolveFirstRunPrimaryAction(activation.data, {
    href: "/getting-started",
    label: "Continue proof-loop milestones"
  });
```

```29:69:/Volumes/DataSSD1/test/periscan/apps/web/src/lib/first-run-primary-action.ts
export function resolveFirstRunPrimaryAction(...) {
  ...
  if (!sourceConnected) return { href: "/integrations", label: "Connect a source", ... };
  if (!scopeVerified) return { href: "/scopes", label: scopePending ? "Finish authorization" : "Authorize scope", ... };
  if (!hasValidation) return { href: "/missions", label: "Run a validation", ... };
  // then activation.nextAction for Act → Verify → Prove
}
```

**Jamie:** Leaves Friday mid-step, Monday still sees one obvious CTA (rail + hero). Low-patience win.

**Residual friction:** `/getting-started` 9-milestone guide + `HeroLoopCoach` after first data still compete with “what do I do next?”

---

### 2. Monday mode exists and is wired — but Shift is a work-queue skin

Operate places Shift next to Home; dashboard deep-links “Open blue shift”; API implements `getBlueShiftBrief` from the product work queue (+ FP reasons).

```368:374:/Volumes/DataSSD1/test/periscan/apps/web/src/lib/primary-nav.tsx
      {
        // P18-2: morning program health — discoverable next to Home / Needs you.
        href: "/shift",
        label: "Shift brief",
        icon: I.signal,
        hint: "Blue shift — morning validated program health"
      },
```

```1471:1522:/Volumes/DataSSD1/test/periscan/apps/api/src/services/tenant.ts
    async getBlueShiftBrief(this: AppServices, context) {
      const [queue, feedback] = await Promise.all([
        this.getProductWorkQueue(context),
        this.listDispositionFeedback(context)
      ]);
      const buckets: BlueShiftBrief["buckets"] = queue.items.map((item) => ({
        count: item.count,
        detail: item.detail,
        href: item.href,
        id: item.itemId,
        title: item.title,
        urgency: item.urgency
      }));
      ...
```

Dashboard “Needs you” is the real triage surface (urgency + stage + deep links):

```684:701:/Volumes/DataSSD1/test/periscan/apps/web/src/components/dashboard-command-center.tsx
      <Panel>
        <div id="needs-you" className="flex flex-col gap-3 p-4 scroll-mt-6">
          ...
              <Link
                href="/shift"
                className="text-[11px] font-medium text-brand hover:underline"
              >
                Open blue shift
              </Link>
```

**Jamie:** Monday laptop path Home → Needs you / Shift is discoverable.  
**Gap:** Shift UI is sparse (`blue-shift-brief.tsx` slate utility styling, load-error as rose text). It re-projects queue buckets rather than a manager brief (owners overdue, “what changed since Friday,” schedule health first). Not wrong — not delightful under low patience.

---

### 3. Mobile app-shell is production-grade; product surfaces are not

```100:145:/Volumes/DataSSD1/test/periscan/apps/web/src/components/app-shell.tsx
        {mobileOpen ? (
          <div ref={mobileDrawerRef} className="fixed inset-0 z-40 md:hidden">
            ...
            <aside
              id={mobileNavId}
              role="dialog"
              aria-modal="true"
              aria-label="Primary navigation"
              className="absolute inset-y-0 left-0 flex w-64 flex-col overflow-y-auto ..."
            >
```

```577:583:/Volumes/DataSSD1/test/periscan/apps/web/src/components/app-shell.tsx
      <button
        ...
        aria-label={mobileNavOpen ? "Close navigation" : "Open navigation"}
        aria-expanded={mobileNavOpen}
        aria-controls={mobileNavId}
        className="min-h-11 min-w-11 ... md:hidden"
```

Also: route closes drawer; focus trap + Escape; main/chrome `inert` under dialogs (`app-shell-nav-scope.test.ts`).

**Guided rail for New / Activating** keeps Jamie’s first week short:

```187:209:/Volumes/DataSSD1/test/periscan/apps/web/src/components/app-shell.tsx
const NEW_TENANT_NAV = new Set([
  "/dashboard",
  "/getting-started",
  "/integrations",
  "/scopes",
  "/runners",
  "/missions",
  "/trust-safety"
]);
const ACTIVATING_TENANT_NAV = new Set([
  ...NEW_TENANT_NAV,
  "/shift",
  "/controls",
  "/attack-paths",
  "/findings",
  "/remediation",
  "/evidence",
  "/reports",
  "/schedules"
]);
```

**Jamie phone reality:** Nav is fine; Findings / Paths / Validate are filter-heavy boards (not reflowed for single-column ops). Phone job = status check + open deep link, not full disposition/rem.

---

### 4. Empty findings / paths — high trust (do not break)

```953:965:/Volumes/DataSSD1/test/periscan/apps/web/src/components/findings-workbench.tsx
        ) : all.length === 0 ? (
          <div className="p-4">
            <NotConfigured
              title="No findings yet"
              message="Findings appear here once a validation run correlates evidence to an exposure."
              action={{ href: "/missions", label: "Run a Validation Snapshot" }}
            />
          </div>
        ) : filtered.length === 0 ? (
          <FilterEmpty
            title="No findings match these filters"
            description="Try another saved view, severity, or search term."
          />
```

```277:293:/Volumes/DataSSD1/test/periscan/apps/web/src/components/attack-paths-workbench.tsx
        ) : all.length === 0 ? (
          <div className="p-4" data-testid="attack-paths-empty">
            <NotConfigured
              title="No attack paths yet — multi-hop measurement starts here"
              message="... empty is honest, not a fake Measured path. We never claim FullyMeasured without hop receipts."
              action={{ href: "/integrations", label: "Connect a source" }}
            />
```

`NotConfigured` uses explicit **“No signal yet”** (`feedback.tsx`) — correct for scanner-weary managers. Empty ≠ clean bill of health.

---

### 5. Act loop still breaks mid-market Monday (path-gated rem + leader persona rail)

**Rem still requires a path in UI** even when many mid-market “findings” are signal/control misses without a multi-hop path:

```1486:1503:/Volumes/DataSSD1/test/periscan/apps/web/src/components/findings-workbench.tsx
  const pathId =
    ...
    if (!pathId || !owner) return;
      const remediation = await api.createRemediation({
        ...
        pathId
```

**Persona trap for “Security leader”** (likely pick for IT security manager): mature allow-list drops Validate (`/missions`) and Scope (`/scopes`):

```274:287:/Volumes/DataSSD1/test/periscan/apps/web/src/components/app-shell.tsx
    SecurityLeader: new Set([
      "/dashboard",
      "/shift",
      "/getting-started",
      "/executive",
      "/attack-paths",
      "/findings",
      "/remediation",
      "/reports",
      "/schedules",
      "/integrations",
      "/mssp",
      "/trust-safety"
    ]),
```

Jamie is hybrid buyer+operator. After maturity, “run another snapshot” / “verify scope” hide behind **Show Labs & more** — low-patience death.

---

## Would adopt mid-market?

**Yes** — as a **design-partner continuous-validation / proof plane** beside existing scanners + SIEM + ITSM, if SE time exists for one measured loop in week one.

**Not yet** as the sole Monday OS for a two-hat IT sec manager until:

1. Findings → rem works without path (`findingFingerprint`)
2. SecurityLeader (manager) rail keeps Validate + Scope on the daily set
3. Shift brief earns its page (not only Home Needs you with different chrome)
4. Phone path is explicitly “review + deep link,” or key triage gets a compact mode

---

## Code evidence map (absolute)

| JTBD | Paths |
|------|--------|
| First-run | `/Volumes/DataSSD1/test/periscan/apps/web/src/components/get-started.tsx`, `dashboard-command-center.tsx`, `welcome-experience.tsx` |
| First-proof resume | `/Volumes/DataSSD1/test/periscan/apps/web/src/lib/first-run-primary-action.ts`, rail CTA in `app-shell.tsx` |
| Monday mode | `/Volumes/DataSSD1/test/periscan/apps/web/app/shift/page.tsx`, `blue-shift-brief.tsx`, `tenant.ts` `getBlueShiftBrief` / `getProductWorkQueue`, dashboard Needs you |
| Mobile shell | `/Volumes/DataSSD1/test/periscan/apps/web/src/components/app-shell.tsx`, `app-shell-nav-scope.test.ts` |
| Empty findings/paths | `findings-workbench.tsx`, `attack-paths-workbench.tsx`, `ui/empty-state.tsx`, `ui/feedback.tsx` (`NotConfigured`) |
| Nav discipline | `/Volumes/DataSSD1/test/periscan/apps/web/src/lib/primary-nav.tsx` |

---

## Score recap (copy block)

```
## P09 Jamie Brooks — IT Sec Manager
**Scores:** task:3.4 clarity:3.5 trust:4.2 delight:3.0 a11y:3.6 purchase:3.3
**Mobile/first-run fit:** B- (shell + GetStarted strong; workbenches desktop-first)
**Top 5 findings:**
1. GetStarted + shared primary CTA = real first-proof resume (keep)
2. Monday Shift + Needs you wired; Shift still thin work-queue projection
3. Mobile nav production-grade; product boards not phone-primary
4. Honest empty findings/paths — high trust
5. Path-gated rem + SecurityLeader rail hide Validate/Scope — Monday / re-run friction
**Would adopt mid-market?** yes (proof plane / design partner — not sole Monday OS yet)
```