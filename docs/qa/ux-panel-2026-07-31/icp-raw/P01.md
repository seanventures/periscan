## P01 Alex Rivera — Security Engineer
**Scores:** task:4 clarity:3 trust:5 delight:3 a11y:4 purchase:4 (1-5 integers)
**Job outcomes:**
- JTBD1 first validation — **pass**: empty Home swaps to a real 3-step spine (source → scope → Validate Snapshot) with activation-backed checkoffs and one primary CTA shared with the rail (`get-started.tsx`, `first-run-primary-action.ts`, `dashboard-command-center.tsx` programStarted gate).
- JTBD2 path measure — **pass**: path detail makes hop fraction, eligibility, and “launch ≠ Measured” explicit; dual column graph+hops / risk+breakers is usable for edge-by-edge work (`attack-path-detail.tsx`).
- JTBD3 findings disposition — **pass**: Monday Active default drops FP/Suppressed, saved views, bulk AR dual-control, expand-in-place disposition (`findings-workbench.tsx`); not a true dual-pane list|detail, so triage is slower than I want on a 14" laptop.
- JTBD4 fix verify — **pass**: ticket ≠ proof, Fixed only after measured re-test, auto-revalidate refuses config-push claims (`remediation-detail.tsx`).
**Top 5 findings:**
1. **P1 | dashboard-command-center.tsx** | Post-first-run Home stacks ProofLoopMap + HeroLoopCoach + multi-hop banner + change lens + 4 metrics + Needs you + charts + path boards — Monday “what do I do now?” competes with ceremony. | Collapse non-Needs-you panels behind “Show program context”; pin Needs you + top path CTA above the fold.
2. **P1 | findings-workbench.tsx** | Disposition is accordion expand under the row, not sticky dual-pane; bulk bar helps, but reviewing impact/evidence/paths while scanning the queue means constant open/close scroll. | List | detail split (select row → right pane keeps DispositionControl / path links / evidence) at `lg+`.
3. **P2 | attack-path-detail.tsx + get-started.tsx** | Two “safe validation” verbs coexist: whole-path “Validate this path (safe)” (approval mission) vs per-hop “Measure hop (safe)” (edge probe). Easy to pick the wrong gate when I only need hop receipts. | One primary path CTA: “Measure hops”; demote full-path validation under secondary “Policy-gated path mission”.
4. **P2 | primary-nav.tsx + app-shell.tsx** | Operate spine is good (Home → … → Schedule), but maturity/`Show Labs & more` still exposes a large product surface; “Validate” honesty vs multi-type missions is correct yet easy to miss under Setup/Labs names. | For `SecurityEngineer` Operating, default rail already in `ENGINEER_DAILY_NAV` — keep Labs hard-hidden; surface Shift brief + Needs you deep links more than Labs.
5. **P2 | attack-path-detail.tsx (NeedsScope hint)** | Hop blocked for scope links to `/missions` with “Verify scope”, while GetStarted correctly sends authorize to `/scopes`. Misroutes the blocker. | Point NeedsScope to `/scopes` (missions only for run).
**Hesitation points:**
1. After first snapshot, whether Home is “done onboarding” or I should still chase proof-loop milestones (`GetStarted` retire vs `/getting-started` + rail CTA).
2. Whether a high risk band on a path means exploitable or still hypothesis until every hop has receipts (`AttackPathClaimBadge` + measuredEdgeCount language is correct — I still re-read it twice).
3. Whether marking remediation ready / creating a ticket will accidentally look “fixed” to leadership (copy says no; habits from other tools say yes).
**Would champion?** **yes** — claim-safe hop measurement, Fixed-only-via-verification, Active/Monday queue, and guided engineer rail match how I run a shift; I’d sell it if first validation stays three clicks and Home stays triage-first instead of map-first.