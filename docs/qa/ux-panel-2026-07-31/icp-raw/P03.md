# P03 Sam Okonkwo — CISO / economic buyer

**Lens:** Leadership proof · trust language · market-presence honesty · compliance disclaimer · executive overview  
**Sources:** code on current tree + panel CISO re-audit (`docs/qa/panel-audit-exhaustive-2026-07-29-rerun/personas/04-ciso.md` — product maps CISO as P04; your ICP ID is P03 Sam).

---

## Scores

| Dimension | Score | Notes |
|-----------|------:|-------|
| **task** | **3.0** | Can open leadership surfaces and export packs, but flagship multi-hop proof + enterprise packaging still block board-as-PoR. |
| **clarity** | **3.2** | Honesty language is excellent; IA buries Executive under Setup; claim/compliance truth is clear when found, not on the first click path. |
| **trust** | **4.2** | Strongest CISO asset: refuse list, zero-ref market presence, NotConfigured residuals, Fixed ≠ ticket-close. |
| **delight** | **2.4** | Trust center is dense questionnaire prose; Executive is “analyst dashboard with print,” not a board narrative. |
| **a11y** | **3.5** | Shell skip-link / single-main / traps improved; Executive/Compliance still light on keyboard/overlay polish. |
| **purchase** | **2.0** | Design-partner pilot only. Not platform-of-record for a 5k regulated shop. |

**Board-room ready?** **no**  
**Would champion purchase?** **no** (would **sponsor a scoped paid pilot** if measured paths + force-MFA + trust-pack fill land; will not champion multi-year PoR)

---

## Top 5 findings

### 1. Honesty architecture is real — and Sam will quote it
Sam’s first question is “will this lie to my board?” The product systematically refuses to invent status:

- **Trust & Safety** (`/Volumes/DataSSD1/test/periscan/apps/web/src/components/trust-safety-dashboard.tsx`): runner outbound-only model, identity plane labeled **Partial — not full IdP lifecycle**, enterprise commercial rows for settlement / SLA / SIEM / multi-region / vendor SOC 2, GTM prove·integrate·refuse panel from `getGtmClaimLanguageSummary()`.
- **Market presence** hard-fails at zero refs: admin banner + trust pack copy (`AdminMarketPresenceBanner`, `buildMarketPresenceReadiness` in shared domain) — Wave/MQ gates **Fail**, banner “Zero customer references…”.
- **Compliance** non-dismissible disclaimer + “(partial)” catalog names (`compliance-workbench.tsx` + `COMPLIANCE_PACK_DISCLAIMER*` in `packages/reports/src/compliance-catalog.ts`).
- **Board packs** separate Fixed from ClosedWithoutEvidence and surface hypothesis mode (`packages/reports/src/index.ts` ExecutiveRiskSummary).

This is rare and **raises trust**. It does **not** by itself close a purchase.

### 2. Executive overview works for ops brief, not risk committee sole source
Code: `/Volumes/DataSSD1/test/periscan/apps/web/src/components/executive-overview.tsx`, route `/executive`.

**What landed (good for Sam’s JTBD):**
- Period selector 30/90/365/all with **real baseline deltas** via `computeExecutivePeriodMetrics` (not the old hardcoded `delta: 0`).
- Decision-confidence banner when metrics/history missing.
- Owner/SLA discipline strip, Closed w/o evidence on velocity bar, CSV + print, link to Reports.
- Summary tab error path for `execError`.

**What still fails board-room bar:**
- “Readiness” ring is **CTEM stage completion math** (OnTrack=100 / NeedsAttention=50), not residual risk or % fully Measured paths.
- Nav placement: **Executive is under Setup**, not Operate (`primary-nav.tsx` ~477–480) — wrong for an economic buyer who lives in leadership proof.
- No first-screen “Measured vs Heuristic path ratio / do-not-certify” banner on the UI (that honesty lives in PDF/HTML packs, not the live overview).
- Multi-hop flagship still **Partial** (panel P04-3 / readiness musts) — anything Sam prints for the committee still needs analyst babysitting.

### 3. Claim-guard exists as contract + Trust panel — not as a always-on “HUD”
There is **no** dedicated `claim-guard-hud` component. Sam’s claim guard is:

| Surface | Path / module |
|---------|----------------|
| GTM prove/integrate/refuse UI | `GtmClaimLanguagePanel` in `trust-safety-dashboard.tsx` |
| Shared deny phrases | `packages/shared/src/gtm-claim-language.ts` (`GTM_DENY_PHRASES`) |
| Path Fixed/Measured physics | `packages/shared/src/claim-language.ts`, `fix-verification.ts` |
| Ontology refuse catalog | `packages/shared/src/claim-deny-list.ts` |

**Win:** code-exported language so SE decks cannot invent “full BAS / ransomware / we make you DORA compliant.”  
**Miss for Sam:** claim truth is not a persistent board HUD on Home/Executive — he must go Admin → Trust & Safety (or know the pack rules). Low-patience CISO will not hunt.

### 4. Compliance workbench disclaims correctly; depth still theater risk
`/Volumes/DataSSD1/test/periscan/apps/web/src/components/compliance-workbench.tsx`:

- Header + yellow note: not certification, not audit opinion, not vendor SOC 2 Type II.
- Wave priority: DORA / NIS2 / PCI first.
- Governance owners / exceptions / sign-off + export PDF from **latest real snapshot**.
- Empty = `NotConfigured` / “never sample claims.”

**Deal residual:** catalogs remain ~3–4 representative controls per framework (`compliance-catalog.ts` “partial, not program-complete”). If GTM says “compliance platform,” Sam’s GRC will reject; if sold as **measured evidence support**, pilot is OK.

### 5. Market presence honesty is purchase-blocking (correctly)
Sam needs peer diligence. Product is explicit:

- Trust pack: “Public customer references = 0… No logos, case studies, or anonymized F500 fiction.”
- Admin zero-refs banner → Trust & Safety.
- Design-partner session log still **0/5** evidence (`docs/DESIGN_PARTNER/SESSION_LEARNING_LOG.md`).

For an economic buyer, **honest zero is better than fake logos** — and still means **no champion PoR signature**.

---

## Deal blockers (what stops Sam / PO)

| Blocker | Why it kills the deal | Evidence |
|---------|----------------------|----------|
| **No peer references / market presence Fail** | Board asks “who else runs this?” | Trust pack + `marketPresence` zero refs |
| **Multi-hop Measured not default / Partial** | Flagship board story incomplete | Panel P04-3; readiness multi-hop Partial |
| **Inbound SCIM / JIT NotConfigured** | 5k IAM will not live on invites forever | `IdentityLifecycleTrustPanel`, SCIM 501 honesty |
| **No independent pen-test letter** | Vendor risk questionnaire stall for in-network runner | `docs/trust/PEN_TEST_ENGAGEMENT.md` |
| **Public SLA / status page NotConfigured** | Cannot list as critical continuous control | `EnterpriseCommercialHonestyPanel` |
| **Vendor SOC 2 Type II NotClaimed** | Legal/procurement default gate | vendor assurance builders in `trust-safety.ts` |
| **SIEM = PullExportOnly (≤5k events)** | Continuous assurance of the tool itself missing | commercial auditStreaming honesty |
| **Default-read RLS residual** | Multi-tenant evidence gold + Labs velocity | threat model / write-RLS honesty |
| **Labs AI surface still large** | Governance risk if shadow-enabled | `primary-nav` Labs group; RFP default excludes Labs (good) |

**Not blockers (strengths):** Fixed-only-via-verification language, denied-not-queued culture, outbound signed runner model, compliance disclaimer discipline, period deltas on Executive.

---

## Surface-by-surface (Sam’s JTBD)

| Surface | Path | Sam’s read |
|---------|------|------------|
| **Executive overview** | `/executive` → `executive-overview.tsx` | Usable **weekly posture brief**; not sole risk-committee source. Period + SLA + closed-w/o-evidence honesty help. Buried under Setup. |
| **Trust & Safety** | `/trust-safety` → `trust-safety-dashboard.tsx` | **Best CISO page in the product** — procurement-grade honesty. Dense; not delightful. |
| **Compliance** | `/compliance` → `compliance-workbench.tsx` | Safe language; thin catalogs. Pilot evidence support, not audit replacement. |
| **Reports / proof composer** | `/reports` → `reports-workbench.tsx` | “Board brief” preset + isolation proof. Leadership-ready **if** snapshot has measured evidence; honesty in pack body. |
| **Claim guard** | Trust GTM panel + shared claim modules | Strong contract; not a persistent HUD. |
| **Market presence** | Admin banner + Trust pack + snapshot design-partner honesty | Explicit Fail at 0 refs — correct and deal-blocking. |
| **Get started** | Dashboard empty → `get-started.tsx` | Excellent **operator** first-run (3 steps + real milestones). **Not Sam’s path** — he expects Executive/Trust, not Connect-a-source. |

---

## Would Sam champion?

| Decision | Posture |
|----------|---------|
| Multi-year platform-of-record | **NO** |
| Paid design-partner pilot (1 BU, success = N fully Measured paths + M Fixed with re-measure IDs) | **CONDITIONAL YES** |
| Replace pen-test / BAS / CNAPP | **NO** — product itself refuses this in GTM deny list |
| Board pack as sole truth | **NO** until multi-hop routine + peer ref |

**Champion purchase overall: no** — he can be a **pilot sponsor**, not a PoR champion.

---

## Code evidence (anchors)

```131:160:/Volumes/DataSSD1/test/periscan/apps/web/src/components/executive-overview.tsx
export function ExecutiveOverview() {
  const exec = useApiResource(() => api.getExecutiveTrends(), []);
  // ...
  // Period-aware deltas via computeExecutivePeriodMetrics; confidenceMessages
```

```477:487:/Volumes/DataSSD1/test/periscan/apps/web/src/lib/primary-nav.tsx
        href: "/executive",
        label: "Executive",
        // ...
        href: "/compliance",
        label: "Compliance",
```
*(both under **Setup**, not Operate)*

```219:234:/Volumes/DataSSD1/test/periscan/apps/web/src/components/compliance-workbench.tsx
      <div role="note" className="...">
        <p>{COMPLIANCE_PACK_DISCLAIMER_SHORT}</p>
        <p>{COMPLIANCE_PACK_DISCLAIMER}</p>
        // not certification / not vendor SOC 2 / not Leading compliance claim
```

```742:747:/Volumes/DataSSD1/test/periscan/apps/web/src/components/trust-safety-dashboard.tsx
      label: "Market presence / customer refs",
      status: "Zero references (fail)",
      detail: "Public customer references = 0. Wave/MQ market presence remains fail..."
```

```9:16:/Volumes/DataSSD1/test/periscan/packages/shared/src/gtm-claim-language.ts
export const GTM_PROVE_CLAIMS = [
  "Measured exposure on verified authorized scope",
  "Measured vs Heuristic path/hop labels in the data model",
  "Fixed only after re-measurement (Fixed can demote)",
  // ...
```

```308:311:/Volumes/DataSSD1/test/periscan/packages/reports/src/index.ts
    case "ExecutiveRiskSummary":
      // Board summary; heuristic paths remain hypotheses;
      // Fixed excludes ClosedWithoutEvidence; not certification
```

---

## Bottom line for overnight-loop

Sam’s **trust score is high** because Periscan refuses to cosplay enterprise maturity it does not have. His **purchase score is low** for the same reason: zero refs, Partial multi-hop, SCIM/SLA/pen-test/vendor Type II gaps, Executive not board-primary.

**To move purchase ≥3.5 and board-room → yes:**  
(1) peer references ≥1 under NDA, (2) fully Measured multi-hop as default demo path, (3) Executive on Operate with Measured/Heuristic board banner, (4) filled trust pack (DPA/subprocessors/pen-test summary), (5) SCIM or contractual provisioning SLA in the order form.