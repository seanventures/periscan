# ICP P08 — Quinn Torres (Red Team Lead)

**Lens:** path measure honesty · no BAS theater · weakest-link · counterfactual estimate-only · DRV Partial · Wave D SOW-only  
**Sources:** `attack-path-detail`, claim-safe display (`AttackPathClaimBadge` + `claim-language.ts`), controls-workbench marker/canary + marker-proof API, `DEMO_OFFENSIVE_GUARDRAILS` / Wave D SOW, competitive matrix/scorecard  
**Mode:** code + docs only (overnight-loop main)

---

## Scores

| Dimension | Score | Quinn read |
|---|---|---|
| **task** | **4.0** | Flagship hop measure → receipt → weakest-edge claim loop is real; DRV is honest Partial (marker class), not library BAS |
| **clarity** | **4.0** | “Heuristic / Partially measured / Measured…” and inject-disabled banner are clear; residual BASLite + dual proof chrome |
| **trust** | **4.5** | Claim clamps, Fixed/receipt rules, safety floors, Wave D SOW gate — rare honesty density vs BAS peers |
| **delight** | **3.0** | Operator path works but is still choreography-heavy; marker-proof is API-strong / UI-split |
| **a11y** | **3.0** | Hop labels/status present; sticky strip + `scrollIntoView` smooth + uneven focus rings remain red-team-console rough |
| **purchase** | **3.5 as complement** / **1.5 as BAS peer** | Buy continuous path/fix proof beside human RT; walk full multi-vector BAS RFP |

**Honesty vs BAS peers:** **High / rare.** Product refuses library inject parity in code and GTM, not only in decks. That earns red-team respect more than another Atomic pack.

**Would respect product?** **Yes** — as AEV/proof layer + purple continuous measurement. **No** if sold as Cymulate/AttackIQ replacement.

---

## Honesty vs BAS peers (one paragraph)

Peers ship scenario-library “Detected” green from dry-run and call it BAS. Periscan hard-disables closed inject (`control_live_execution_disabled`), defaults Atomic dry-run to `NoEvidence` + `ValidationScenarioImported` + `measured: false`, remaps overclaimed path Validated→claim-safe via hop receipts, and documents Wave D inject as **SOW + dual approval + default off**. DRV is labeled **Partial** (benign-marker Fully-E2E only). Quinn’s standard is: *don’t lie about what you measured*. This repo mostly doesn’t.

---

## Top 5 findings

### 1. Path certainty is weakest-link and claim-safe — flagship honesty landed
**Why it matters to Quinn:** Risk score must never upgrade path certainty; Measured without evidence IDs is theater.

**Evidence:**
- `/Volumes/DataSSD1/test/periscan/packages/shared/src/claim-language.ts` — `edgeCountsAsMeasured` requires `Measured` **and** evidence IDs; `projectPathValidationState` remaps Reachable/Validated/Exploitable → `Discovered` when hops don’t support the claim; `deriveAttackPathClaim` refuses exploitable/validated/reachable unless **fully** measured.
- `/Volumes/DataSSD1/test/periscan/apps/web/src/ui/state-badge.tsx` — `AttackPathClaimBadge` shows claim `displayLabel` (e.g. “Heuristic hypothesis”), not raw workflow Validated; title discloses recorded vs evidence certainty.
- `/Volumes/DataSSD1/test/periscan/apps/web/src/components/attack-path-detail.tsx` — “Weakest-edge certainty”; “Launch alone never upgrades a hop”; hop cards + receipts.
- `/Volumes/DataSSD1/test/periscan/packages/evidence/src/edge-receipts.ts` — `recomputeAttackPathFromReceipts` writes claim-safe `validationState`; `receiptMarksMeasured` blocks upgrade on `Inconclusive`/`NoEvidence`.
- Reports: `/Volumes/DataSSD1/test/periscan/packages/reports/src/index.ts` — claim-safe state + remap reason on export.

**Residual:** No separate file named `claim-safe-display`; claim-safe **is** `AttackPathClaimBadge` + `projectPathValidationState`. Any client that renders raw `AttackPath.validationState` without the badge can still overclaim (Einstein residual: dual recorded vs claim-safe).

---

### 2. Multi-hop Measure is no longer API-only theater (receipt apply closed the loop)
**Why it matters:** Prior red-team audit (P05-1) said UI never applied receipts. Overnight loop fixed both auto-apply and UI apply.

**Evidence:**
- `/Volumes/DataSSD1/test/periscan/apps/api/src/services/findings.ts` — `tryAutoApplyPathEdgeReceiptFromCompletedRun` (P05-1); identity import forced `Inconclusive` / never Measured.
- `/Volumes/DataSSD1/test/periscan/apps/api/src/runtime-services.ts` ~11419 — control-plane hop completion auto-applies receipts.
- `/Volumes/DataSSD1/test/periscan/apps/web/src/components/attack-path-detail.tsx` — polls mission runs; “Apply hop receipt from mission” requires `Completed` + evidence IDs; refuses empty evidence (“real-first”).
- Identity honesty: `/Volumes/DataSSD1/test/periscan/packages/evidence/src/edge-validation-plan.ts` eligibility `HeuristicOnly`; UI copy: re-import never mints Measured.

**Residual:** APV matrix still correctly **Partial** — chaining is mostly heuristic; FullyMeasured multi-hop end-to-end remains the exception, not the demo default.

---

### 3. Controls: inject theater refused; marker/canary is limited; Wave B DRV is API-first
**Why it matters:** Quinn will open Controls first and look for BAS green-wash.

**Evidence (good):**
- `/Volumes/DataSSD1/test/periscan/apps/web/src/components/controls-workbench.tsx` — banner: inject loop not available (`control_live_execution_disabled`); Atomic dry-run only; exact-marker canary is “limited safe stimulus — not full BAS.”
- Exact-marker canary: authorize → dispatch → exact marker correlate → verdict; technique match alone never earns Detected (copy + flow ~548–676).
- Atomic honesty: `/Volumes/DataSSD1/test/periscan/packages/modules/src/index.ts` — name “not live inject”; `fixtureOutcome ?? "NoEvidence"`; `ValidationScenarioImported`; `measured: false`; live path returns `live_execution_disabled`.
- DRV product path: `/Volumes/DataSSD1/test/periscan/apps/api/src/services/control-ai.ts` `runDetectionMarkerProof` — allowlisted `periscan-*` marker; `drvClaimClass: "benign_marker_only"`; summary states not full ATT&CK BAS.
- Route: `/Volumes/DataSSD1/test/periscan/apps/api/src/app.ts` `POST /api/v1/control-sources/:id/detection-marker-proof`.
- Matrix: `/Volumes/DataSSD1/test/periscan/docs/COMPETITIVE_COVERAGE_MATRIX.md` — SCV Partial; DRV Partial (benign-marker Fully-E2E only).
- Scorecard demoted SCV to Partial with honesty note (`docs/qa/analyst-scorecard.json` id 6).

**Finding (gap):** Web has **no** caller for `detection-marker-proof` / `runDetectionMarkerProof` (grep across `apps/web` empty). Operator-visible path is **exact-marker HTTP canary**, not the signed emit→observe DRV loop. Quinn will call that “API real, product half-shipped.”

**Finding (skepticism):** Marker proof injects mock SIEM observation when `injectMockObservation` unset and (`fixtureMode` or `devMode`) with empty events (`control-ai.ts` ~1249–1262). Dev green-path is convenient; SE misuse risk remains if demos run API without liveTelemetry discipline.

---

### 4. Wave D closed inject is correctly SOW-only / default off — refuse theater
**Evidence:**
- `/Volumes/DataSSD1/test/periscan/docs/competitive/DEMO_OFFENSIVE_GUARDRAILS.md` § “Wave D closed inject — SOW-only (default off)”: SOW required, dual approval, dry-run default, never ransomware/spray/SharpHound/Caldera/Atomic live library; claim: one governed inject loop ≠ scenario-library BAS.
- `/Volumes/DataSSD1/test/periscan/docs/qa/FULL_MATRIX_COVERAGE_AGENTIC_WAVES.md` Wave D still SOW-gated; hard stop list.
- `/Volumes/DataSSD1/test/periscan/SECURITY_BOUNDARIES.md` — safety floor not to erode for BAS peer pressure.

**Quinn verdict:** Respect. Do not start Wave D without real SOW; do not market “optional lab inject” as product parity.

---

### 5. Counterfactual / money is estimate-only (no invented $)
**Evidence:**
- `/Volumes/DataSSD1/test/periscan/apps/web/src/components/attack-path-detail.tsx` — “Financial exposure · **assumption-based**”; empty state: “Periscan will not invent a dollar value.”
- `/Volumes/DataSSD1/test/periscan/packages/shared/src/business-impact.ts` — scenarios require named sources/review; not random ALE.

**Quinn verdict:** Pass. Board-facing $ without assumptions would be the fastest way to lose this ICP.

---

## Additional residual (not top 5, but on Quinn’s list)

| Item | Evidence |
|---|---|
| **BASLite** still in scope/control UX | `controls-workbench.tsx` “Verified BASLite-capable scope”; enum in modules Atomic `safetyLevel: "BASLite"` |
| Matrix lag: multi-vector says DNS-exfil modules missing | Matrix row vs real `periscan.dns_exfil_canary` in `packages/modules` |
| Dual proof chrome | `ProofStageStrip` sticky + full `ProofLoopContext` on path detail |
| Feature zoo still exists beyond RT JTBD | Swarm/MCP/gateway nav residual (prior panels); honesty docs tell SEs to start on `/shift` not Swarm |
| a11y | `scrollIntoView({ behavior: "smooth" })` without reduced-motion; focus often border-only on workbenches |

---

## Code map (what Quinn would open in a design-partner session)

| JTBD | Where |
|---|---|
| Path measure honesty | `apps/web/src/components/attack-path-detail.tsx`, `packages/shared/src/claim-language.ts`, `packages/evidence/src/edge-receipts.ts` |
| Claim-safe display | `apps/web/src/ui/state-badge.tsx` → `AttackPathClaimBadge` |
| Marker / DRV Partial | Controls exact-marker canary UI; API `runDetectionMarkerProof` + `periscan.detection_marker_emit_observe` |
| No BAS theater | `controls-workbench` inject banner; Atomic module; `claim-deny-list.ts` refuse `full-bas-peer` |
| Wave D SOW | `docs/competitive/DEMO_OFFENSIVE_GUARDRAILS.md`, wave plan Wave D |
| Counterfactual estimate-only | Path financial exposure panel; `business-impact` schemas |

---

## Bottom line for Quinn

| Question | Answer |
|---|---|
| Path measure honest? | **Yes** — weakest hop + evidence IDs + claim remap; receipts auto + UI |
| BAS theater? | **Mostly no** — inject disabled, Atomic import-only, deny-list, SOW Wave D |
| DRV? | **Partial as claimed** — benign marker class real (API); UI canary separate; not full rule library |
| Counterfactual? | **Estimate-only** — no invented dollars |
| Wave D? | **SOW-only, default off** — correct |
| Respect product? | **Yes** as continuous proof beside red team |
| Buy full BAS seat budget? | **No** — refuse that RFP; buy for hop/fix/re-proof honesty |

**Net score (composite, Quinn lens): ~3.8 / 5.0** — respect high, purchase as **complement** only. Overnight loop closed the multi-hop receipt hole that previously made flagship AEV look like API theater; remaining skepticism is marker-proof UI productization, BASLite naming, mock-observe defaults in dev, and matrix lag — not “they fake Validated.”