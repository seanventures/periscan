# Periscan Predeployment Checklist

Status as of 2026-06-28 on `codex/runner-mtls-certificate-alignment`. Gate results are from actual local runs against the test Postgres on port 5432, including the latest full `pnpm verify` after the runner mTLS certificate-alignment slice.

| Item                                     | Status | Evidence / note                                                                                                                                                                                                                          |
| ---------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Install works                            | ✅     | `pnpm install` (pnpm 9.15, node ≥20)                                                                                                                                                                                                     |
| `.env.example` complete                  | ✅     | All core/security/ops/runner/queue/model keys present                                                                                                                                                                                    |
| Required secrets listed                  | ✅     | See `deploy-requirements.md`; prod refuses dev-key fallbacks (v0.1.125)                                                                                                                                                                  |
| Migrations present + documented          | ✅     | `prisma migrate deploy`; schema under `packages/db/prisma`                                                                                                                                                                               |
| Seed/demo documented                     | ✅     | `pnpm seed:demo`; demo surfaces explicitly labeled "sample data"                                                                                                                                                                         |
| Build passes                             | ✅     | `pnpm verify` includes `pnpm build`: Next.js web, API, worker, runner-agent, and all packages built successfully.                                                                                                                        |
| Lint passes                              | ✅     | `pnpm verify` includes `pnpm lint` across all workspace projects.                                                                                                                                                                        |
| Typecheck passes                         | ✅     | `pnpm verify` includes Prisma generate plus workspace TypeScript checks.                                                                                                                                                                 |
| Unit tests pass                          | ✅     | `pnpm verify` includes the full workspace Vitest suite.                                                                                                                                                                                  |
| Integration/app tests pass               | ✅     | API, web, connectors, modules, worker, model-gateway, reports, evidence, policy, operators, and webhook package tests pass in the workspace suite.                                                                                       |
| Security tests pass                      | ✅     | `pnpm verify` includes `pnpm test:security`: 2 files / 22 tests.                                                                                                                                                                         |
| Acceptance tests pass                    | ✅     | `pnpm verify` includes `pnpm test:acceptance`: 100 files / 123 tests.                                                                                                                                                                    |
| Web unit/component tests                 | ✅     | `pnpm verify` includes `@periscan/web` Vitest coverage: 35 files / 168 tests.                                                                                                                                                            |
| e2e (playwright)                         | ✅     | `pnpm verify` includes Playwright E2E and accessibility checks: 58/58 tests passed locally with Postgres and Redis available.                                                                                                            |
| Auth configured                          | ✅     | session JWT + API keys (scope→role, fail-closed); prod cookie-secure + CORS env                                                                                                                                                          |
| Permissions enforced server-side         | ✅     | requireRole on writes (share-link gap fixed v0.1.141); tenant-scoped reads                                                                                                                                                               |
| Error handling reviewed                  | ✅     | AppServiceError→status mapping; bad-cookie 401 not 500                                                                                                                                                                                   |
| Logging reviewed                         | ✅     | structured logs for mission/sync/policy; sweep failure attribution                                                                                                                                                                       |
| Observability documented                 | ✅     | `/metrics` + prometheus gauges; readiness sweep health                                                                                                                                                                                   |
| Secrets at rest                          | ✅     | AES-256-GCM for integration + model creds; redacted on read (manifest-authoritative v0.1.142)                                                                                                                                            |
| DB indexes/pagination                    | ✅     | cursor pagination (listMissions); indexed threat/audit/path queries                                                                                                                                                                      |
| Rate limits / abuse controls             | ✅     | global + auth rate limits; external-validation rate limiter                                                                                                                                                                              |
| Evidence integrity                       | ✅     | sha256 recompute on read (v0.1.122)                                                                                                                                                                                                      |
| Kill switches                            | ✅     | runner kill switch enforced; model-gateway kill switch cancels all in-flight (v0.1.143)                                                                                                                                                  |
| Audit export honesty                     | ✅     | truncation reported (v0.1.139)                                                                                                                                                                                                           |
| Snapshot measured posture wiring         | ✅     | `POST /api/v1/snapshots` now runs built-in measured DNS/TLS/HTTP/email posture modules for verified Domain/Subdomain scopes before report generation; fixture-only in dev mode, live-safe in production                                  |
| Fix verification measured posture wiring | ✅     | External-exposure remediation verification now selects measured DNS/TLS/HTTP/email modules, uses verified-scope hostname targets, records ActiveNonInvasive mission safety, and marks validation-module retests as measured revalidation |
| Snapshot report accessible states        | ✅     | Snapshot report preview is a named region, route API work exposes `aria-busy`, and an empty report API body offers retry/workspace recovery; component suite covers these states                                                         |
| Global web shell breadcrumbs             | ✅     | Root layout now renders a route-aware breadcrumb from the shared primary navigation contract, including dynamic Snapshot report routes; component and Playwright shell gates cover it                                                    |
| MSSP mobile grid polish                  | ✅     | Portfolio metric and client coverage grids now collapse to one column first, then expand at `sm`/`lg`; component tests assert the mobile-first classes                                                                                   |
| Non-MSSP mobile metric grids             | ✅     | API Health, Signal Activity, Findings, Validation Ops, Registry Center, and ATT&CK Catalog metric grids now collapse to one column at the base breakpoint with component regressions                                                     |
| CI status                                | ⏳     | `scripts/verify.sh` is the gate and passed locally on 2026-06-28; confirm hosted GitHub Actions once account billing/spending-limit issues are resolved before production deploy.                                                        |
| Migration history integrity              | ✅     | `prisma migrate deploy` = no pending migrations after applying 69 tracked migrations; drift backfilled in v0.1.153                                                                                                                       |
| Rollback/migration notes                 | ✅     | documented in deploy-requirements.md (additive-only to date; forward-compatible image rollback; no destructive migrations)                                                                                                               |
| Build artifact cleanup                   | ✅     | `pnpm clean:build` removes generated `dist/` directories under `apps/` and `packages/`; current workspace cleanup verified with `find apps packages -type d -name dist`                                                                  |
| Demo scenario breadth                    | ✅     | `pnpm seed:demo` now seeds GitHub/AWS/Jira/Splunk, AI app validation, missed-control validation, Snapshot, remediation tickets, and fix-verification events for all seeded remediations                                                  |
| MSSP white-label branding UI             | ✅     | `/mssp` now reads/saves tenant report branding through `GET/PUT /api/v1/tenants/current/branding` and previews persisted white-label report fields; component suite covers the API-backed edit/save flow                                 |

## Open predeployment items (loop backlog)

- No in-repo predeployment backlog items remain open in this checklist. Continue fresh scans against PRD/spec before release claims.

## Blockers requiring human input (not code-fixable)

- Live connector/model credentials (customer-supplied).
- Actual deploy authorization + target environment provisioning.
- Live BAS/runner execution legal sign-off.
