# Periscan .ai Release Readiness

**Current addendum:** 2026-06-28, inspected `codex/runner-mtls-certificate-alignment` after the runner mTLS certificate-alignment slice and the previous integrated readiness stack, Real-First API hardening, full route shell/accessibility coverage, production DB/web/Redis readiness hardening, API Reference contract completion, Trust & Safety integration readiness metadata, tenant webhook lifecycle audit, API test build-artifact isolation, connector health truthfulness, and release-status reconciliation. This supersedes the `v0.1.334` wording where it differs.

- **Additional capability now ready:** public global threat-intelligence super-feed: 13 feed adapters, global deduped catalog, feed source health, high-frequency poller, SSRF-guarded fetcher, tenant-scoped realtime alerts, alert status updates, `/threat-feed` web surface, and verified-scope correlation for exact domain, parent-domain/subdomain, IPv4 CIDR, and tracked CVEs.
- **Commercial/private threat intel remains a decision:** the previous "External threat feed" owner decision should now be read as commercial/private vendor selection beyond the implemented public super-feed and existing read-only threat-intel connectors.
- **Release metadata current:** GitHub Release `v0.1.346` is the current pushed `main` release. The runner mTLS certificate-alignment branch is a post-release candidate branch and should not be described as released until merged and tagged. Historical divergent tag refs still should not be force-updated blindly.
- **Current verification:** Full local `pnpm verify` passed on 2026-06-28 after the runner mTLS certificate-alignment slice with local Postgres at `127.0.0.1:5432`, including lint, typecheck, workspace tests, production build, Docker-backed Go runner tests, runner lab, OSS toolchain, license checks, PRD audit gate, Prisma migrate deploy, Playwright E2E 58/58, security 22/22, high+ audit gate with one low-severity advisory below the fatal threshold, production dependency audit, and acceptance 100 files / 123 tests. GitHub-hosted checks previously could not start because GitHub reported account billing/spending-limit failures; release confidence remains based on local verification until hosted CI billing is resolved.

---

**Date:** 2026-06-17 (refreshed at v0.1.334 — supersedes the stale 2026-06-05/v0.1.47 assessment)
**Method:** 4-dimension skeptical audit of the ACTUAL v0.1.334 code on `main` (core proof-loop, production/ops, feature completeness, test/gate/doc honesty), each verified against source — not docs. Two audit imprecisions were re-verified and corrected (see "Audit corrections" below).

## Verdict

**Periscan is production-ready in-repo at v0.1.334.** The core promise ("validate what is REAL") is genuinely enforced, there are no in-repo production blockers, and ~85% of the product is built-and-real with the remaining ~15% being clean slots that await **your decisions** (payment provider, IdP, threat-feed vendor, legal sign-offs, deploy target) — not half-built stubs. The path to a first paying customer is a **deployment + go-to-market exercise plus a short list of external decisions**, not more in-repo building.

## Genuinely ready (verified against source)

- **Core proof-loop is honest.** Fix-verification cannot reach "Fixed" without `executedRealRetest=true` AND `evidenceBasis="Measured"` AND the path no longer re-correlates (apps/api/src/runtime-services.ts buildVerificationResult ~6051; risk.ts toBand early-return ~112). Heuristic correlation paths are flagged `heuristic:true` / `validationState:"Discovered"`, never "Validated" (packages/evidence/src/correlation.ts). AI endpoint probes return `Inconclusive` at the result and run level, never `Validated`/`Passed`, and are not promoted into validated findings or Snapshot AI risk lists without a real Promptfoo/PyRIT/Garak harness result. Connector mock observers default to neutral verdicts (SIEM "Logged", EDR/WAF "NoEvidence"/"Missed") after v0.1.326/331/333. Invariants are guarded by tests/security/validation-provenance.test.ts plus module/runtime truthfulness tests.
- **Deployable today.** Real multi-stage Dockerfiles (api/worker/web/runner-agent/runner), 47 additive hand-authored migrations (none destructive), AES-256-GCM secret encryption, Ed25519 signed runner tasks, HS256 JWT + httpOnly cookies, TOTP MFA (+recovery), RBAC, tenant isolation on every query, per-tenant rate limiting, strict security headers (HSTS prod-only), graceful shutdown, queue backpressure, webhook retries/dead-letter, job/runner-task reapers, health/ready endpoints, structured logging + request IDs.
- **Feature-complete core.** 43 validation modules (real tool execution separated from fixtureMode), 121+ connectors (real code; live needs customer creds), complete internal runner (enroll → signed-task poll → execute → signed result → kill switch → credential rotation), billing metering **with enforced quotas** (`requireCapability` → 402 + audit), MFA fully wired in API + web, governed model-gateway/autonomous-engagement MVP, signal-trigger continuous validation, 19 API-driven web surfaces.
- **Tests/gates/docs are honest.** No skipped tests; acceptance suite (65 files) hits real Postgres/Redis/MinIO; security boundary suite is real (cross-tenant denial, nonce replay, policy binding, kill switch, redaction); CI **never** runs live offensive tools (fixtureMode only); license gate fail-closed; `pnpm audit --audit-level high` is fatal.

## Audit corrections (two flagged "gaps" were already covered)

- **Go runner IS built + tested in CI.** `ci.yml` runs `pnpm verify` → `scripts/test-runner.sh`, which runs `gofmt` check + `go test ./...` + `go build ./...` + a Docker image build with non-root-user (65532) + `--help` smoke (golang:1.22-alpine fallback if host Go absent; ubuntu-latest ships both Go and Docker). `runner-publish.yml` publishes the image to GHCR on `v*` tags. No gap.
- **Cross-tenant evidence reads ARE explicitly tested.** `tests/security/security-boundaries.test.ts` covers owner access and cross-tenant denial for `GET /api/v1/evidence/:id`, `GET /api/v1/evidence/:id/download`, and `GET /api/v1/attack-paths/:id/evidence`.

## What's left for a first paying customer

### Your decisions / external (cannot be resolved in-repo)

| Item                               | Needed                                                                                                                                                | Current in-repo state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Payment                            | Pick a processor (Stripe/Paddle/…) + checkout integration                                                                                             | Metering + quota ENFORCEMENT already built; `paymentProcessorStatus:"NotConfigured"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| SSO/SAML/OIDC                      | Pick/configure the customer IdP, redirect URI/entity ID, OIDC client credentials or SAML IdP certificate, and any customer-specific claim conventions | API-first generic OIDC/SAML SSO is implemented: tenant-admin CRUD/disable, encrypted write-only OIDC client secret, write-only SAML IdP certificate response semantics, sanitized reads, OIDC auth URL generation, SAML SP metadata, public start/callback endpoints, OIDC JWKS/issuer/audience/nonce/email-domain validation, SAML signed-response/request-correlation validation, session creation for provisioned members, enforced-password-login denial, password/legacy session tenant-switch denial for enforced-SSO tenants, same-tenant SSO enforcement for cross-tenant switches into enforced tenants, and audit events. |
| Commercial/private threat feed     | Pick a vendor if the public super-feed and read-only intel connectors are insufficient                                                                | Public super-feed is implemented; manual advisory import works; commercial/private feed onboarding remains a customer/business decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| SharpHound / Caldera / Atomic live | Legal sign-off + policy override                                                                                                                      | Deliberately policy-denied; governance + module surface exist                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Deployment                         | Host + provisioned Postgres/Redis/S3/TLS + secret rotation                                                                                            | Dockerfiles + prod compose + `db:migrate:deploy` ready                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

### In-repo (our control) — minor, optional

- AI endpoint-probe nomenclature is closed: benign endpoint probes now keep result evidence `Inconclusive`, are excluded from validated findings/Snapshot AI risk lists, and remain available through run/evidence provenance.
- Optional defense-in-depth on secondary evidence routes is complete: `/evidence/:id/download` and `/attack-paths/:id/evidence` now have explicit cross-tenant denial coverage in the security boundary suite.
- Go runner CI toolchain pinning is closed on branch `codex/ci-go-toolchain-pin`: CI installs the Go version from `apps/runner/go.mod` before `pnpm verify`, and local `pnpm test:runner` falls back to the same Go 1.22 Docker image when local Go is absent or too old.

## Deployment-managed (operator's job, not code blockers)

Managed Postgres (HA/PITR), managed Redis (persistence), S3-compatible evidence storage (versioning/encryption), TLS/domains, secret rotation (`PERISCAN_JWT_SECRET`, S3, Redis), log aggregation + alert routing targets, evidence retention policy, and per-customer live connector credentials + verified scope + in-network runner deployment.

## Sign-off status

- [x] Full `pnpm verify` is the CI gate (lint, typecheck, unit, build, Go runner, tools:check, license, prisma migrate:deploy, e2e, security, acceptance).
- [x] No open P0/P1 in-repo gaps for first-customer scope.
- [x] Core proof-loop honesty invariants enforced + test-guarded.
- [x] Go runner + cross-tenant evidence read verified covered (audit corrections above).
- [ ] External decisions (payment / IdP / threat feed / legal) — owner action.
- [ ] Production deployment executed in a real environment — owner action.

**Do not** claim live customer validation for integrations/targets/runners that have not been credentialed and authorized in that customer environment.

_Update on every material change._
