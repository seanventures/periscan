import { ReportsWorkbench } from "../../src/components/reports-workbench";

export const metadata = {
  title: "Reports — Periscan"
};

export default function ReportsPage() {
  return <ReportsWorkbench />;
}
