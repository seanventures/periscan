import { ControlsWorkbench } from "../../src/components/controls-workbench";

export const metadata = {
  title: "Controls — Periscan"
};

export default function ControlsPage() {
  return <ControlsWorkbench />;
}
