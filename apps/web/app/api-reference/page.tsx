import { ApiReferenceConsole } from "../../src/components/api-reference-console";

export const metadata = {
  title: "API Reference — Periscan"
};

export default function ApiReferencePage() {
  return <ApiReferenceConsole />;
}
