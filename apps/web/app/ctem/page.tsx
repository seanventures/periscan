import { CtemProgramBoard } from "../../src/components/ctem-program-board";

export const metadata = {
  title: "CTEM proof program — Periscan"
};

export default function CtemProgramPage() {
  return <CtemProgramBoard />;
}
