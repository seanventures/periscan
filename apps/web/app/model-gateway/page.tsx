import { FrontierGatewayConsole } from "../../src/components/frontier-gateway-console";

export const metadata = {
  title: "Frontier Gateway — Periscan"
};

export default function ModelGatewayPage() {
  return <FrontierGatewayConsole />;
}
