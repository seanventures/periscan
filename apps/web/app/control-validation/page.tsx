import { ControlValidationWorkbench } from "../../src/components/control-validation-workbench";

export const metadata = {
  title: "Control validation — Periscan"
};

export default function ControlValidationPage() {
  return <ControlValidationWorkbench />;
}
