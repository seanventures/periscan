import { SchedulesWorkbench } from "../../src/components/schedules-workbench";

export const metadata = {
  title: "Schedules — Periscan"
};

export default function SchedulesPage() {
  return <SchedulesWorkbench />;
}
