import { AtomicTestingWorkbench } from "../../../src/components/atomic-testing-workbench";

export const metadata = {
  title: "Atomic testing — Periscan"
};

export default function AtomicTestingPage() {
  return <AtomicTestingWorkbench />;
}
