import { BasOperatorWorkspace } from "../../src/components/bas-operator-workspace";

export const metadata = {
  title: "BAS operator workspace — Periscan"
};

export default function BasOperatorWorkspacePage() {
  return <BasOperatorWorkspace />;
}
