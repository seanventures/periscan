import { RunnerFleetControlRoom } from "../../src/components/runner-fleet-control-room";

export const metadata = {
  title: "Runners — Periscan"
};

export default function RunnersPage() {
  return <RunnerFleetControlRoom />;
}
