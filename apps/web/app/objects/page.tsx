import { ObjectExplorerCatalog } from "../../src/components/object-explorer";

export const metadata = {
  title: "Object explorer — Periscan"
};

export default function ObjectsPage() {
  return <ObjectExplorerCatalog />;
}
