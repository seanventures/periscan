import { AgentWorkflowStudio } from "../../src/components/agent-workflow-studio";

export const metadata = {
  title: "Agent Workflows — Periscan"
};

export default function WorkflowsPage() {
  return <AgentWorkflowStudio />;
}
