import { FindingsWorkbench } from "../../src/components/findings-workbench";

export const metadata = {
  title: "Findings — Periscan"
};

export default function FindingsPage() {
  return <FindingsWorkbench />;
}
