import { DashboardCommandCenter } from "../../src/components/dashboard-command-center";

export const metadata = {
  title: "Dashboard — Periscan"
};

export default function DashboardPage() {
  return <DashboardCommandCenter />;
}
