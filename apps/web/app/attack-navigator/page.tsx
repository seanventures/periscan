import { AttackNavigatorWorkbench } from "../../src/components/attack-navigator-workbench";

export const metadata = {
  title: "ATT&CK Navigator overlay — Periscan"
};

export default function AttackNavigatorPage() {
  return <AttackNavigatorWorkbench />;
}
