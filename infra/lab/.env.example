# Copy to .env (gitignored) when using runner profile
LAB_PROFILE=exposed
CONTROL_PLANE_URL=http://host.docker.internal:3001
PERISCAN_RUNNER_IMAGE=ghcr.io/seanheiney/periscan-runner:latest
# Issued by control plane — never commit real tokens
PLANT_TOKEN=
PLANT_RUNNER_ID=
HQ_TOKEN=
HQ_RUNNER_ID=
